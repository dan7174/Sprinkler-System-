"""Deterministic, unit-tested irrigation calculation library.

Every function is a pure function: same inputs always produce the same
output, invalid inputs raise ValueError instead of guessing. Formula
sources are cited in each module and registered in
knowledge/source_manifest.yaml.
"""
